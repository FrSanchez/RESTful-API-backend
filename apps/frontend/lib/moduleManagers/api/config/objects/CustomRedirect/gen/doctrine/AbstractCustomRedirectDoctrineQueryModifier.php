<?php
namespace Api\Config\Objects\CustomRedirect\Gen\Doctrine;

use Api\Objects\FieldDefinition;
use Api\Objects\Doctrine\QueryBuilderNode;
use RuntimeException;

/**
 * DO NOT MODIFY! This is generated by the API generation suite. Use "baker-api-gen" to generate a new version.

 */
abstract class AbstractCustomRedirectDoctrineQueryModifier extends \Api\Objects\Doctrine\DoctrineQueryModifier
{
	/**
	 * Override this method to add any additional fields or join for the derived fields selected by the user. Usually
	 * getValueForDerivedField will also need to be overridden to handle calculating the derived field's value.
	 *
	 * @param QueryBuilderNode $queryBuilderNode
	 * @param FieldDefinition[] $derivedFieldDefinitions
	 */
	public function modifyQueryBuilderWithDerivedFields(QueryBuilderNode $queryBuilderNode, array $derivedFieldDefinitions): void
	{
		foreach ($derivedFieldDefinitions as $fieldDef) {
			switch ($fieldDef->getName()) {
				case 'bitlyIsPersonalized':
				case 'bitly_is_personalized':
					$this->modifyQueryWithBitlyIsPersonalizedField($queryBuilderNode, $fieldDef);
					break;
				case 'bitlyShortUrl':
				case 'bitly_short_url':
					$this->modifyQueryWithBitlyShortUrlField($queryBuilderNode, $fieldDef);
					break;
				default:
					throw new RuntimeException("Unhandled derived field: {$fieldDef->getName()}.");
			}
		}
			}

	/**
	 * @param \Doctrine_Record $doctrineRecord
	 * @param FieldDefinition $fieldDef
	 * @return mixed
	 */
	protected function getValueForDerivedField(\Doctrine_Record $doctrineRecord, FieldDefinition $fieldDef)
	{
		switch ($fieldDef->getName()) {
			case 'bitlyIsPersonalized':
			case 'bitly_is_personalized':
				return $this->getValueForBitlyIsPersonalizedField($doctrineRecord, $fieldDef);
			case 'bitlyShortUrl':
			case 'bitly_short_url':
				return $this->getValueForBitlyShortUrlField($doctrineRecord, $fieldDef);
			default:
				return parent::getValueForDerivedField($doctrineRecord, $fieldDef);
		}
			}

	/**
	 * Override this method to add any additional fields or joins to calculate the bitlyIsPersonalized field. You should also override
	 * the getValueForBitlyIsPersonalizedField function to handle calculating the bitlyIsPersonalized value.
	 *
	 * @param QueryBuilderNode $queryBuilderRoot
	 * @param FieldDefinition $fieldDef
	 * @see getValueForBitlyIsPersonalizedField
	 */
	protected abstract function modifyQueryWithBitlyIsPersonalizedField(QueryBuilderNode $queryBuilderRoot, FieldDefinition $fieldDef);

	/**
	 * @param \Doctrine_Record $doctrineRecord
	 * @param FieldDefinition $fieldDef
	 * @return mixed
	 * @see modifyQueryWithBitlyIsPersonalizedField
	 */
	protected abstract function getValueForBitlyIsPersonalizedField(\Doctrine_Record $doctrineRecord, FieldDefinition $fieldDef);

	/**
	 * Override this method to add any additional fields or joins to calculate the bitlyShortUrl field. You should also override
	 * the getValueForBitlyShortUrlField function to handle calculating the bitlyShortUrl value.
	 *
	 * @param QueryBuilderNode $queryBuilderRoot
	 * @param FieldDefinition $fieldDef
	 * @see getValueForBitlyShortUrlField
	 */
	protected abstract function modifyQueryWithBitlyShortUrlField(QueryBuilderNode $queryBuilderRoot, FieldDefinition $fieldDef);

	/**
	 * @param \Doctrine_Record $doctrineRecord
	 * @param FieldDefinition $fieldDef
	 * @return mixed
	 * @see modifyQueryWithBitlyShortUrlField
	 */
	protected abstract function getValueForBitlyShortUrlField(\Doctrine_Record $doctrineRecord, FieldDefinition $fieldDef);

}
