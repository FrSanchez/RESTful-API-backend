<?php
namespace Api\Config\Objects\Prospect\Gen\Doctrine;

use Api\Objects\FieldDefinition;
use Api\Objects\Doctrine\QueryBuilderNode;
use RuntimeException;

/**
 * DO NOT MODIFY! This is generated by the API generation suite. Use "baker-api-gen" to generate a new version.

 */
abstract class AbstractProspectDoctrineQueryModifier extends \Api\Objects\Doctrine\DoctrineQueryModifier
{
	/**
	 * Override this method to add any additional fields or join for the derived fields selected by the user. Usually
	 * getValueForDerivedField will also need to be overridden to handle calculating the derived field's value.
	 *
	 * @param QueryBuilderNode $queryBuilderNode
	 * @param FieldDefinition[] $derivedFieldDefinitions
	 */
	public function modifyQueryBuilderWithDerivedFields(QueryBuilderNode $queryBuilderNode, array $derivedFieldDefinitions): void
	{
		foreach ($derivedFieldDefinitions as $fieldDef) {
			switch ($fieldDef->getName()) {
				case 'recentInteraction':
				case 'recent_interaction':
					$this->modifyQueryWithRecentInteractionField($queryBuilderNode, $fieldDef);
					break;
				case 'salesforceCampaignId':
				case 'crm_campaign_fid':
					$this->modifyQueryWithSalesforceCampaignIdField($queryBuilderNode, $fieldDef);
					break;
				default:
					throw new RuntimeException("Unhandled derived field: {$fieldDef->getName()}.");
			}
		}
			}

	/**
	 * @param \Doctrine_Record $doctrineRecord
	 * @param FieldDefinition $fieldDef
	 * @return mixed
	 */
	protected function getValueForDerivedField(\Doctrine_Record $doctrineRecord, FieldDefinition $fieldDef)
	{
		switch ($fieldDef->getName()) {
			case 'recentInteraction':
			case 'recent_interaction':
				return $this->getValueForRecentInteractionField($doctrineRecord, $fieldDef);
			case 'salesforceCampaignId':
			case 'crm_campaign_fid':
				return $this->getValueForSalesforceCampaignIdField($doctrineRecord, $fieldDef);
			default:
				return parent::getValueForDerivedField($doctrineRecord, $fieldDef);
		}
			}

	/**
	 * Override this method to add any additional fields or joins to calculate the recentInteraction field. You should also override
	 * the getValueForRecentInteractionField function to handle calculating the recentInteraction value.
	 *
	 * @param QueryBuilderNode $queryBuilderRoot
	 * @param FieldDefinition $fieldDef
	 * @see getValueForRecentInteractionField
	 */
	protected abstract function modifyQueryWithRecentInteractionField(QueryBuilderNode $queryBuilderRoot, FieldDefinition $fieldDef);

	/**
	 * @param \Doctrine_Record $doctrineRecord
	 * @param FieldDefinition $fieldDef
	 * @return mixed
	 * @see modifyQueryWithRecentInteractionField
	 */
	protected abstract function getValueForRecentInteractionField(\Doctrine_Record $doctrineRecord, FieldDefinition $fieldDef);

	/**
	 * Override this method to add any additional fields or joins to calculate the salesforceCampaignId field. You should also override
	 * the getValueForSalesforceCampaignIdField function to handle calculating the salesforceCampaignId value.
	 *
	 * @param QueryBuilderNode $queryBuilderRoot
	 * @param FieldDefinition $fieldDef
	 * @see getValueForSalesforceCampaignIdField
	 */
	protected abstract function modifyQueryWithSalesforceCampaignIdField(QueryBuilderNode $queryBuilderRoot, FieldDefinition $fieldDef);

	/**
	 * @param \Doctrine_Record $doctrineRecord
	 * @param FieldDefinition $fieldDef
	 * @return mixed
	 * @see modifyQueryWithSalesforceCampaignIdField
	 */
	protected abstract function getValueForSalesforceCampaignIdField(\Doctrine_Record $doctrineRecord, FieldDefinition $fieldDef);

}
